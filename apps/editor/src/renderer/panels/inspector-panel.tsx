import type { IDockviewPanelProps } from 'dockview';
import type { ReactElement } from 'react';

// Placeholder. Property editing for the selected entity lands in a later work package.
export function InspectorPanel(_props: IDockviewPanelProps): ReactElement {
  return <div className="panel-placeholder">Inspector</div>;
}
