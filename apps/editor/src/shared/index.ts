// Isomorphic editor-shared: the IPC contract (channel names + Zod schemas). Built in WP-0.2.
// May import: zod, @marionette/format/types only. Stays runtime-free so it cannot poison the
// sandboxed preload or renderer.
export {};
