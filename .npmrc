strict-peer-dependencies=false
auto-install-peers=true
engine-strict=false
resolution-mode=highest

# Skip the pre-run install check (it hard-fails on ERR_PNPM_IGNORED_BUILDS in pnpm 11).
# The Electron binary is installed; running scripts must not trigger a blocking reinstall.
verify-deps-before-run=false
