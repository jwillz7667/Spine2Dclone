node_modules
dist
out
coverage
.turbo
pnpm-lock.yaml
*.tsbuildinfo

# Prose and docs are hand-authored, not prettier-formatted: the em-dash guard
# (tools/check-no-dashes.mjs) enforces the one hard rule, and prettier is non-idempotent
# on some planning docs. Prettier owns code and config, not markdown.
docs/
**/*.md

# Plain-text legal and git config files: no prettier parser exists for them.
LICENSE
NOTICE
.gitattributes
.github/CODEOWNERS

# Generated, byte-asserted golden fixture (serialized via JSON.stringify and compared
# byte-for-byte in golden.test.ts; reformatting it would break that drift guard).
packages/runtime-core/test/golden/

# Generated conformance fixtures + lock manifest. generate.ts owns their exact bytes (compact raw
# affines, shortest round-trippable floats), and the fixtures-lock drift gate is a byte-exact git diff
# (conformance-and-ci.md A.6); reformatting them would break that gate. The hand-authored rig and
# sample-spec under packages/conformance/src are prettier-owned and intentionally NOT excluded.
packages/conformance/src/fixtures/

# Generated particle (effects) conformance fixtures + lock manifest (phase-3-vfx-particles.md WP-3.10).
# generate-effects.ts owns their exact bytes (shortest round-trippable floats) and the
# .effects-fixtures.lock drift gate is a byte-exact git diff; reformatting them would break that gate.
# The hand-authored effects rigs and sample-specs alongside are prettier-owned and NOT excluded.
packages/conformance/src/effects-fixtures/

# Generated slot golden-playback timelines + lock manifest (phase-4-slot-composer.md WP-4.13). generate-slot.ts
# owns their exact bytes and the .slot.fixtures.lock drift gate is a byte-exact git diff; reformatting them
# would break that gate. The hand-authored spins, scenes, and sample-specs under slot/ (spins/, scenes/,
# sample-spec/) are prettier-owned and intentionally NOT excluded.
packages/conformance/src/slot/expected/

# Generated AnimationState (anim-state) pose-capture goldens + lock manifest (ADR-0005). generate-anim-state.ts
# owns their exact bytes (shortest round-trippable floats) and the .anim-state-fixtures.lock drift gate is a
# byte-exact git diff; reformatting them would break that gate. The hand-authored rig and scenarios under
# anim-state-rigs/ and anim-state-scenarios/ are prettier-owned and intentionally NOT excluded.
packages/conformance/src/anim-state-fixtures/

# Generated demo artifacts (heavy, machine-written)
demo/diamond-fusion/atlas/
demo/diamond-fusion/boards.json
demo/diamond-fusion/player/index.html
demo/gunner/atlas/
demo/gunner/renders/
demo/gunner/rigs/
demo/gunner/source-layers/
demo/gunner/source-manifest.json
demo/gunner/player/index.html
demo/leprechaun-gold/atlas/
demo/leprechaun-gold/layout.json
demo/leprechaun-gold/player/index.html
demo/kraken/atlas/
demo/kraken/renders/
demo/kraken/source/
demo/kraken/player/index.html
demo/kraken/krakens-hoard.rig.json
demo/little-aliens/atlas/
demo/little-aliens/atlas-fx/
demo/little-aliens/renders/
demo/little-aliens/source/
demo/little-aliens/source-layers/
demo/little-aliens/source-fx/
demo/little-aliens/player/index.html
demo/little-aliens/little-aliens.rig.json
demo/little-aliens/little-aliens.effects.json
demo/little-aliens/source-manifest.json
slot-assets/

# Native build artifacts (gitignored; excluded so a local dotnet build does not pollute format:check)
runtimes/**/bin/
runtimes/**/obj/
