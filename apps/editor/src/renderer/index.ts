// Renderer process: React + dockview panels, PixiJS viewport, document model + commands.
// Built from WP-0.2 onward. May import: runtime-web, runtime-core, format, react, pixi.js,
// zustand, dockview, editor-shared (types). Never the main or preload process code.
export {};
